const input = document.getElementById('input').value;
const output = document.getElementById('output').value;
// const key = document.getElementById('key').textContent.split('');

console.log(input);

function encrypt(input, key) {
  let result = "";
  const textArray = input.replace(/\s/g, '').split('');
    for (let i=0; i<textArrray.length; i++) {
      textArray[i].charCodeAt(0) + 1;
    }
}
